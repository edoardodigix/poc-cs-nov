_schema-version: "3.2"
ID: poccsnov
description: Generated by Fiori Tools
version: 0.0.1
modules:
- name: poccsnov-destination-content
  type: com.sap.application.content
  requires:
  - name: poccsnov-destination-service
    parameters:
      content-target: true
  - name: poccsnov-repo-host
    parameters:
      service-key:
        name: poccsnov-repo-host-key
  - name: poccsnov-uaa
    parameters:
      service-key:
        name: poccsnov-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: poccsnov_html_repo_host
          ServiceInstanceName: poccsnov-html5-srv
          ServiceKeyName: poccsnov-repo-host-key
          sap.cloud.service: poccsnov
        - Authentication: OAuth2UserTokenExchange
          Name: poccsnov_uaa
          ServiceInstanceName: poccsnov-xsuaa-srv
          ServiceKeyName: poccsnov-uaa-key
          sap.cloud.service: poccsnov
        existing_destinations_policy: ignore
  build-parameters:
    no-source: true
- name: poccsnov-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: poccsnov-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - poccsnov.zip
      name: poccsnov
      target-path: resources/
- name: poccsnov
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: poccsnov-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: poccsnov-destination-service
    service-plan: lite
- name: poccsnov-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: poccsnov-xsuaa-srv
    service-plan: application
- name: poccsnov-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: poccsnov-html5-srv
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
